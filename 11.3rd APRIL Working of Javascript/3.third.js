// log the variable using var keyword before assigging the value we get  -  undefined
// when let used insted of var error occured is - reference error

sum(6, 4);

function sum(num1, num2) {
  console.log(ans);
  var ans = num1 + num2;
}
